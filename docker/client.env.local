# ── Convex Self-Hosted Configuration ────────────────────────────────────────
# When running inside Docker, the backend is reachable via service name
CONVEX_SELF_HOSTED_URL=http://backend:3210
CONVEX_SELF_HOSTED_ADMIN_KEY=<run ./setup.sh --generate-admin-key after docker up>

# When running locally (outside Docker), use:
# CONVEX_SELF_HOSTED_URL=http://127.0.0.1:3210
